CACHE MANIFEST
#last changed: <?php echo $this->lastChanged; ?>

#conjoon <?php echo $this->conjoonVersion; ?>

CACHE:
<?php
for ($i = 0, $len = count($this->fileList); $i < $len; $i++) {
    $m = trim(file_get_contents($this->fileList[$i]));
    if ($m) {
        echo str_replace("../../", "/", $m) . "\n";
    }
}
?>

NETWORK:
*