Install notes:
==================
Move this folder to any location on your disc drive, then adjust the
"application_path" variable from the "environment" section found in the
file config.ini.php. Preferable, the path should not be within the
document root, rather in a directory that any user cannot access
directly.
Make sure the folder is readable and writable by the webserver.